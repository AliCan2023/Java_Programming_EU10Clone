package day44_AbstractionContinue_Interface.AnimalTask;

public interface WildAnimal {
    boolean isWild = true; //static & final by default;

     void hunt();// abstract method by default ;it is optional to use abstract keyword interface but a MUST in abstract class;

    }

